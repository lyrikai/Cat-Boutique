<!DOCTYPE html>
<html>
  <head>
    <title>Catshop</title>
    
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <%= javascript_importmap_tags %>
    <script src="https://js.stripe.com/v3/"></script> 
  </head>

  <body>
 <header class="d-flex justify-content-center py-3">
      <ul class="nav nav-pills">
        <li class="nav-item"><a href=<%= root_path %> class="nav-link active" aria-current="page">Home</a></li>
        <% if user_signed_in? %>
        <li class="nav-item"><a href="profils/show" class="nav-link">Profil</a></li>
        <li class="nav-item"><a href=<%= destroy_user_session_path%> class="nav-link">Sign out</a></li>
        <li class="nav-item"><a href=<%= carts_path%> class="nav-link">Cart</a></li>

        <% if current_user.admin? %>
        <li class="nav-item"><a href="/admin" class="btn btn-success">Ajouter un produit</a></li>
        <% end %>        
        <% else %>
        <li class="nav-item"><a href=<%=new_user_session_path %> class="nav-link">Sign in</a></li>
        <li class="nav-item"><a href=<%=new_user_registration_path %> class="nav-link">Sign up</a></li>
        <% end %>
      </ul>
    </header>
    <%= yield %>
  </body>
</html>
